{% extends 'ConfigOrionBundle::layout.html.twig' %}
{% block body%}

<h1>Editar propiedad del sistema</h1>

<div class="content-hr-both show-content">
    <form action="{{ path('sistema_update', { 'id': document.id }) }}" method="post" {{ form_enctype(edit_form) }}  class="form-horizontal" role="form">
        <table class="table table-hover">
            <tr>
                <th>Nombre</th>
                <td>{{ document.nombre }}</td>
            </tr>
            <tr>
                <th>Descripci&oacute;n</th>
                <td>{{ document.descripcion }}</td>
            </tr>
            <tr>
                <th>{{ form_label(edit_form.valor) }}</th>
                <td>{{ form_errors(edit_form.valor) }}{{ form_widget(edit_form.valor) }}</td>
            </tr>
        </table>
        <div class="hidden">{{ form_rest(edit_form) }}</div>
        <p class="actions-forms">
            <button type="submit" class="btn btn-success active">
                <span class="glyphicon glyphicon-ok"></span> Guardar
            </button>
        </p>
    </form>
</div>

<div class="show-actions">
    <a class="btn btn-default active" role="button" href="{{ path('sistema') }}">
        <span class="glyphicon glyphicon-th-list"></span> Lista de propiedades del sistema
    </a>
    {#
    <button class="btn btn-danger active" data-toggle="modal" data-target="#modalDelete">
        <span class="glyphicon glyphicon-remove"></span> Eliminar
    </button>

    <div class="modal fade" id="modalDelete" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                    <h4 class="modal-title" id="myModalLabel">Antenci&oacute;n!!!</h4>
                </div>
                <div class="modal-body">
                    ¿Est&aacute; seguro que desea eliminar este dato?
                </div>
                <div class="modal-footer">
                    <form action="{{ path('sistema_delete', { 'id': document.id }) }}" method="post">
                        {{ form_widget(delete_form) }}
                        <button type="submit" class="btn btn-danger">
                            <span class="glyphicon glyphicon-remove"></span> Si
                        </button>
                    </form>
                    <button type="button" class="btn btn-default" data-dismiss="modal">No</button>
                </div>
            </div><!-- /.modal-content -->
        </div><!-- /.modal-dialog -->
    </div><!-- /.modal -->
    #}

</div>


{% endblock%}