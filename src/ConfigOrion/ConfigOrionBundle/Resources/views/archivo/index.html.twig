{% extends 'ConfigOrionBundle::layout.html.twig' %}
{% block body%}

<h1>Lista de archivos</h1>

<div class="content-hr-both">
    <div class="panel panel-default">
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Nombre</th>
                    <th>Ruta</th>
                    <th>Formato</th>
                    <th>Contenido</th>
                    <th>Descripci&oacute;n</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
            {% for document in documents %}
                <tr>
                    <td><a href="{{ path('archivo_show', { 'id': document.id }) }}">{{ document.id }}</a></td>
                    <td>{{ document.nombre }}</td>
                    <td>{{ document.ruta }}</td>
                    <td>{{ document.formato }}</td>
                    <td>{{ document.contenido }}</td>
                    <td>{{ document.descripcion }}</td>
                    <td>        <a href="{{ path('archivo_show', { 'id': document.id }) }}"><span class="glyphicon glyphicon-eye-open"></span></a>
    &nbsp;
                        <a href="{{ path('archivo_edit', { 'id': document.id }) }}"><span class="glyphicon glyphicon-edit"></span></a>
    &nbsp;
        
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
</div>

<div class="show-actions">
    <a class="btn btn-primary active" role="button" href="{{ path('archivo_new') }}">
        <span class="glyphicon glyphicon-plus"></span> Crear archivo
    </a>
</div>

{% endblock%}