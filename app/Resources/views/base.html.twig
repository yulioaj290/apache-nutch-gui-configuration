<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="description" content="">
        <meta name="author" content="">
        <title>
            {% block title %}
            CONFIG-ORI&Oacute;N | Sistema para la configuraci&oacute;n del mecanismo de rastreo de Ori&oacute;n
            {% endblock %}
        </title>

        <!-- Importando CSS -->       
        {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('bundles/configorion/css/style.css') }}" type="text/css" />
        <link rel="stylesheet" href="{{ asset('bundles/configorion/css/bootstrap.min.css') }}" type="text/css" />
        <link rel="stylesheet" href="{{ asset('bundles/configorion/css/jquery-ui.css') }}" type="text/css" />

        <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
        <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
        <!--[if lt IE 9]>
            <script src="{{ asset('bundles/configorion/js/html5shiv.js') }}"></script>
            <script src="{{ asset('bundles/configorion/js/excanvas.js') }}" type="text/javascript"></script>
        <![endif]-->

            {% block specific_stylesheets %}{% endblock %}
            {% block inline_css %}{% endblock %}
        {% endblock %}

        <!-- Favicon de la APP -->  
        <link rel="shortcut icon" href="{{ asset('bundles/configorion/ico/favicon.ico') }}">
    </head>
    <body>
        <noscript>
        <h1>Debe ACTIVAR el JavaScript de su Navegador</h1>
        </noscript>        
        {% block nav_bar %}{% endblock %}

        {% block flash_bags %}{% endblock %}

        <div class="body-general-aplication">{% block body %}{% endblock %}</div>

        <div class="footer">
            <p>Copyright &copy; {{ 'now'|date('Y') }} - Universidad de las Ciencias Inform&aacute;ticas<br />Sistema para la configuraci&oacute;n del mecanismo de rastreo de Ori&oacute;n 2.0 | Motor de B&uacute;squeda Cubano
        </div>

        {% block javascripts %}
        <!-- Others javascript files -->
        <script src="{{ asset('bundles/configorion/js/script.js') }}" type="text/javascript"></script>
        <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
        <script src="{{ asset('bundles/configorion/js/jquery.min.js') }}" type="text/javascript"></script>
        <script src="{{ asset('bundles/configorion/js/jquery-ui.js') }}" type="text/javascript"></script>
        <!-- Include all compiled plugins (below), or include individual files as needed -->
        <script src="{{ asset('bundles/configorion/js/bootstrap.min.js') }}" type="text/javascript"></script>
        <script src="{{ asset('bundles/configorion/js/tooltip.js') }}" type="text/javascript"></script>
        <script src="{{ asset('bundles/configorion/js/popover.js') }}" type="text/javascript"></script>

        {% block specific_javascripts %}{% endblock %}
        {% block inline_javascripts %}{% endblock %}
        
        <script type='text/javascript' >
            $(document).ready(function() {
                if ($("[rel=tooltip]").length) {
                    $("[rel=tooltip]").tooltip();
                }
            });
        </script>
        {% endblock %}
    </body>
</html>
